<%= simple_form_for(@committee) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :committee_name %>
    <%= f.input :responsibilities %>
    <%= f.association :contract %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
